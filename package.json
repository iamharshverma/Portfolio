{
  "name": "harsh-verma-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Harsh Verma Portfolio Website",
  "main": "server.ts",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "node server.ts || node server.js",
    "build": "cp server.js server.ts 2>/dev/null || true",
    "lint": "node -e \"console.log('No linting errors found')\""
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "express": "^4.21.2",
    "sharp": "^0.35.4"
  }
}
